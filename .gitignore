# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.import_linter_cache/

# uv
# (uv.lock IS committed — it pins the whole workspace)

# Env / secrets
.env
*.db

# Eval run log (RC1-248). Local runs are noise; RC1-255 decides whether CI runs
# get committed as artifacts to render the trend view from.
eval-runs/

# Node / Vite (apps/web)
node_modules/
dist/
.vite/

# OS / editor
.DS_Store
.idea/
.vscode/
