{
  "name": "launch-planner-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive Gantt UI for launch-planner-agent (frappe-gantt + Vite).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "frappe-gantt": "0.6.1"
  },
  "devDependencies": {
    "sass-embedded": "^1.100.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.7"
  }
}
